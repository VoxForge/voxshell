% NS_B
<s>

% NS_E
</s>

# using empty command return values
% FILLER
[] UH WELL HUM

% ID
[] COMPUTER

% SHELL_COMMANDS
# compile_grammar.jl will expand the pronunciation of compound words; this may
# be a problem if the triphones do not exist (Julius tells you when this
# happens).  You may need to add your own *logical* to *physical* triphone mapping
# to tiedlist file (not as complicated as it sounds...)
#[@GRAMMAR_ROOT@/shell/scripts/ruby_weather.rb] 
[@GRAMMAR_ROOT@/shell/scripts/weather.sh] WEATHER IS-IT-CLOUDY IS-IT-SUNNY
[firefox -remote openurl(https://ca-mg6.mail.yahoo.com/neo/launch?.partner=rogers-acs)] EMAIL MAIL
[ls] LIST-FILES FILES
# if want to execute shell command with certain parameters (like '*'), need to put it in a bash script
[@GRAMMAR_ROOT@/shell/scripts/listdirs.sh] LIST-DIRECTORIES DIRECTORY-LIST DIRECTORIES
[pwd] PRINT-WORKING-DIRECTORY
[ls bin] LIST-BIN BIN

% VOLUME
[] VOLUME

% VOLUME_CAT
[pactl set-sink-volume 1 -- +20%] UP LOUDER
[pactl set-sink-volume 1 -- -20%] DOWN LOWER SOFTER

